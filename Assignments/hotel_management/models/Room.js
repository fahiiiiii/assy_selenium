const express = require('express');
const app = express();
const bodyParser = require('body-parser');
app.use = bodyParser.json();
export default  Room = {
        hotelSlug: string,
        roomSlug: string,
        roomImage: string,
        roomTitle: string,
        bedroomCount: number
    }
    